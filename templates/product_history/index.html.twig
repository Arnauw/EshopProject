{% extends 'base.html.twig' %}

{% block title %}ProductHistory index{% endblock %}

{% block body %}
    <h1>ProductHistory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>CreatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product_history in product_histories %}
            <tr>
                <td>{{ product_history.id }}</td>
                <td>{{ product_history.quantity }}</td>
                <td>{{ product_history.createdAt ? product_history.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_product_history_show', {'id': product_history.id}) }}">show</a>
                    <a href="{{ path('app_product_history_edit', {'id': product_history.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_product_history_new') }}">Create new</a>
{% endblock %}
