{% extends 'base.html.twig' %}

{% block title %}ProductHistory{% endblock %}

{% block body %}
    <h1>ProductHistory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_history.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ product_history.quantity }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ product_history.createdAt ? product_history.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_history_index') }}">back to list</a>

    <a href="{{ path('app_product_history_edit', {'id': product_history.id}) }}">edit</a>

    {{ include('product_history/_delete_form.html.twig') }}
{% endblock %}
